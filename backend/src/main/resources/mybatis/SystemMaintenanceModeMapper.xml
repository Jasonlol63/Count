<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eazycount.dao.SystemMaintenanceModeDao">

    <select id="findEnabled" resultType="java.lang.Boolean">
        SELECT enabled FROM system_maintenance_mode WHERE id = 1
    </select>

    <update id="updateEnabled">
        UPDATE system_maintenance_mode SET enabled = #{enabled} WHERE id = 1
    </update>
</mapper>
