<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eazycount.dao.DashboardDao">

    <!-- RATE 主腿限定：排除同 rate_group 下的中间人手续费行（与 TransactionSearchMapper 的
         rateTransferLegOnly 同一份逻辑，按本项目惯例在各 mapper 里各自维护一份拷贝） -->
    <sql id="dashboardRateTransferLegOnly">
        (
            t.transaction_type &lt;&gt; 'RATE'
            OR EXISTS (
                SELECT 1
                FROM transactions_rate tr
                WHERE tr.tenant_id = t.tenant_id
                  AND tr.rate_group_id = t.rate_group_id
                  AND (t.id = tr.leg1_transaction_id OR t.id = tr.leg2_transaction_id)
            )
        )
    </sql>

    <!-- RATE 中间人手续费行（旧版 RATE_MIDDLEMAN_FEE 或 MARKUP X … 文本）——
         与 TransactionSearchMapper 的 rateMiddlemanFeeDescription 同一份逻辑 -->
    <sql id="dashboardRateMiddlemanFeeDescription">
        (
            t.description = 'RATE_MIDDLEMAN_FEE'
            OR t.description LIKE 'MARKUP X %'
        )
    </sql>

    <!-- RATE 中间人手续费行：同 rate_group 但非 leg1/leg2 —— 与 TransactionSearchMapper 的
         rateMiddlemanFeeLeg 同一份逻辑 -->
    <sql id="dashboardRateMiddlemanFeeLeg">
        EXISTS (
            SELECT 1
            FROM transactions_rate tr
            WHERE tr.tenant_id = t.tenant_id
              AND tr.rate_group_id = t.rate_group_id
              AND tr.middleman_account_id IS NOT NULL
              AND t.id &lt;&gt; tr.leg1_transaction_id
              AND t.id &lt;&gt; tr.leg2_transaction_id
        )
    </sql>

    <!-- Win/Loss 桶：WIN(+)/LOSE(-)/ADJUSTMENT(原样) 记在 account_id 一侧；
         手动 PROFIT 类型转账 To(-)/From(+)；RATE 中间人手续费 +amount（自己的收入，非 Cr/Dr）；
         按 role 汇总；按币种过滤 —— 不同货币的 amount 不能直接相加，只统计 currencyCode 这一种货币 -->
    <select id="aggregateWinLossByRole" resultType="com.eazycount.dto.DashboardKpiDTO$RoleAmount">
        SELECT x.role AS role, COALESCE(SUM(x.amt), 0) AS amount
        FROM (
            SELECT
                a.role AS role,
                CASE
                    WHEN t.transaction_type = 'WIN' THEN t.amount
                    WHEN t.transaction_type = 'LOSE' THEN -t.amount
                    WHEN t.transaction_type = 'ADJUSTMENT' THEN t.amount
                    ELSE 0
                END AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type IN ('WIN', 'LOSE', 'ADJUSTMENT')
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            SELECT a.role AS role, -t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'PROFIT'
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            SELECT a.role AS role, t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.from_account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.from_account_id IS NOT NULL
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'PROFIT'
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            <!-- RATE 中间人手续费单边行：account_id=middleman, from_account_id 为空 → +amount -->
            SELECT a.role AS role, t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'RATE'
              AND t.from_account_id IS NULL
              AND <include refid="dashboardRateMiddlemanFeeDescription"/>
              AND <include refid="dashboardRateMiddlemanFeeLeg"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            <!-- RATE 中间人手续费旧版两边行：from_account_id=middleman → +amount
                 （例如 "Rate charge (x2.93) from SGD 49000.00" 这种旧文案，没有 rate_group 主腿标记，
                 只能靠 transactions_rate.middleman_account_id 反查） -->
            SELECT a.role AS role, t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.from_account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.from_account_id IS NOT NULL
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'RATE'
              AND <include refid="dashboardRateMiddlemanFeeLeg"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>
        ) x
        GROUP BY x.role
    </select>

    <!-- Cr/Dr 桶：PAYMENT/CLAIM/CONTRA/RATE(仅主腿) To(-)/From(+)；RATE 中间人手续费的付款方那一侧
         -amount（与上面 Win/Loss 桶的中间人 +amount 是同一笔手续费的两个视角，不重复也不遗漏）；
         按 role 汇总；CLEAR 故意不在类型列表里 —— Transaction List 页正常显示/结清 CLEAR，
         但 Dashboard 的 PROFIT/EXPENSES KPI 永远排除 CLEAR（业务规则，见分析记录）；按币种过滤，理由同上 -->
    <select id="aggregateCrDrByRole" resultType="com.eazycount.dto.DashboardKpiDTO$RoleAmount">
        SELECT x.role AS role, COALESCE(SUM(x.amt), 0) AS amount
        FROM (
            SELECT a.role AS role, -t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type IN ('PAYMENT', 'CLAIM', 'CONTRA', 'RATE')
              AND <include refid="dashboardRateTransferLegOnly"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            SELECT a.role AS role, t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.from_account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.from_account_id IS NOT NULL
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type IN ('PAYMENT', 'CLAIM', 'CONTRA', 'RATE')
              AND <include refid="dashboardRateTransferLegOnly"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            <!-- RATE 中间人手续费付款方视角：account_id=付款方、from_account_id 非空 → -amount -->
            SELECT a.role AS role, -t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.from_account_id IS NOT NULL
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'RATE'
              AND <include refid="dashboardRateMiddlemanFeeLeg"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>
        ) x
        GROUP BY x.role
    </select>

    <!-- Win/Loss 桶（Group Profit 汇总用）：跟 aggregateWinLossByRole 完全同一套规则，只是多按
         tenant_id 分一层组——一条 SQL 直接拿到 Group 下每家公司各自的数字，不用按公司循环查 -->
    <select id="aggregateWinLossByRoleAndTenant" resultType="com.eazycount.dto.DashboardKpiDTO$RoleAmount">
        SELECT x.tenantId AS tenantId, x.role AS role, COALESCE(SUM(x.amt), 0) AS amount
        FROM (
            SELECT
                t.tenant_id AS tenantId,
                a.role AS role,
                CASE
                    WHEN t.transaction_type = 'WIN' THEN t.amount
                    WHEN t.transaction_type = 'LOSE' THEN -t.amount
                    WHEN t.transaction_type = 'ADJUSTMENT' THEN t.amount
                    ELSE 0
                END AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type IN ('WIN', 'LOSE', 'ADJUSTMENT')
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            SELECT t.tenant_id AS tenantId, a.role AS role, -t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'PROFIT'
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            SELECT t.tenant_id AS tenantId, a.role AS role, t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.from_account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.from_account_id IS NOT NULL
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'PROFIT'
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            <!-- RATE 中间人手续费单边行：account_id=middleman, from_account_id 为空 → +amount -->
            SELECT t.tenant_id AS tenantId, a.role AS role, t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'RATE'
              AND t.from_account_id IS NULL
              AND <include refid="dashboardRateMiddlemanFeeDescription"/>
              AND <include refid="dashboardRateMiddlemanFeeLeg"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            <!-- RATE 中间人手续费旧版两边行：from_account_id=middleman → +amount -->
            SELECT t.tenant_id AS tenantId, a.role AS role, t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.from_account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.from_account_id IS NOT NULL
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'RATE'
              AND <include refid="dashboardRateMiddlemanFeeLeg"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>
        ) x
        GROUP BY x.tenantId, x.role
    </select>

    <!-- Cr/Dr 桶（Group Profit 汇总用）：跟 aggregateCrDrByRole 完全同一套规则，只是多按
         tenant_id 分一层组 -->
    <select id="aggregateCrDrByRoleAndTenant" resultType="com.eazycount.dto.DashboardKpiDTO$RoleAmount">
        SELECT x.tenantId AS tenantId, x.role AS role, COALESCE(SUM(x.amt), 0) AS amount
        FROM (
            SELECT t.tenant_id AS tenantId, a.role AS role, -t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type IN ('PAYMENT', 'CLAIM', 'CONTRA', 'RATE')
              AND <include refid="dashboardRateTransferLegOnly"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            SELECT t.tenant_id AS tenantId, a.role AS role, t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.from_account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.from_account_id IS NOT NULL
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type IN ('PAYMENT', 'CLAIM', 'CONTRA', 'RATE')
              AND <include refid="dashboardRateTransferLegOnly"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            <!-- RATE 中间人手续费付款方视角：account_id=付款方、from_account_id 非空 → -amount -->
            SELECT t.tenant_id AS tenantId, a.role AS role, -t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.from_account_id IS NOT NULL
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'RATE'
              AND <include refid="dashboardRateMiddlemanFeeLeg"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>
        ) x
        GROUP BY x.tenantId, x.role
    </select>

    <!-- Win/Loss 桶（Group Trend Chart 用）：跟 aggregateWinLossByRoleAndTenant 完全同一套规则，
         再多按 transaction_date 分一层组——知道"每家子公司每一天自己赚了多少"，喂给 Group Profit
         那条走势线（每天的子公司 NetProfit 乘以当月股权% 再加总） -->
    <select id="aggregateWinLossByRoleAndTenantAndDate" resultType="com.eazycount.dto.DashboardTrendPointDTO$RoleAmount">
        SELECT x.tenantId AS tenantId, x.date AS date, x.role AS role, COALESCE(SUM(x.amt), 0) AS amount
        FROM (
            SELECT
                t.tenant_id AS tenantId,
                t.transaction_date AS date,
                a.role AS role,
                CASE
                    WHEN t.transaction_type = 'WIN' THEN t.amount
                    WHEN t.transaction_type = 'LOSE' THEN -t.amount
                    WHEN t.transaction_type = 'ADJUSTMENT' THEN t.amount
                    ELSE 0
                END AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type IN ('WIN', 'LOSE', 'ADJUSTMENT')
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            SELECT t.tenant_id AS tenantId, t.transaction_date AS date, a.role AS role, -t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'PROFIT'
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            SELECT t.tenant_id AS tenantId, t.transaction_date AS date, a.role AS role, t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.from_account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.from_account_id IS NOT NULL
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'PROFIT'
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            <!-- RATE 中间人手续费单边行：account_id=middleman, from_account_id 为空 → +amount -->
            SELECT t.tenant_id AS tenantId, t.transaction_date AS date, a.role AS role, t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'RATE'
              AND t.from_account_id IS NULL
              AND <include refid="dashboardRateMiddlemanFeeDescription"/>
              AND <include refid="dashboardRateMiddlemanFeeLeg"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            <!-- RATE 中间人手续费旧版两边行：from_account_id=middleman → +amount -->
            SELECT t.tenant_id AS tenantId, t.transaction_date AS date, a.role AS role, t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.from_account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.from_account_id IS NOT NULL
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'RATE'
              AND <include refid="dashboardRateMiddlemanFeeLeg"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>
        ) x
        GROUP BY x.tenantId, x.date, x.role
    </select>

    <!-- Cr/Dr 桶（Group Trend Chart 用）：跟 aggregateCrDrByRoleAndTenant 完全同一套规则，
         再多按 transaction_date 分一层组 -->
    <select id="aggregateCrDrByRoleAndTenantAndDate" resultType="com.eazycount.dto.DashboardTrendPointDTO$RoleAmount">
        SELECT x.tenantId AS tenantId, x.date AS date, x.role AS role, COALESCE(SUM(x.amt), 0) AS amount
        FROM (
            SELECT t.tenant_id AS tenantId, t.transaction_date AS date, a.role AS role, -t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type IN ('PAYMENT', 'CLAIM', 'CONTRA', 'RATE')
              AND <include refid="dashboardRateTransferLegOnly"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            SELECT t.tenant_id AS tenantId, t.transaction_date AS date, a.role AS role, t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.from_account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.from_account_id IS NOT NULL
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type IN ('PAYMENT', 'CLAIM', 'CONTRA', 'RATE')
              AND <include refid="dashboardRateTransferLegOnly"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            <!-- RATE 中间人手续费付款方视角：account_id=付款方、from_account_id 非空 → -amount -->
            SELECT t.tenant_id AS tenantId, t.transaction_date AS date, a.role AS role, -t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.from_account_id IS NOT NULL
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'RATE'
              AND <include refid="dashboardRateMiddlemanFeeLeg"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>
        ) x
        GROUP BY x.tenantId, x.date, x.role
    </select>

    <!-- Win/Loss 桶（Trend Chart 用）：跟 aggregateWinLossByRole 完全同一套规则，只是多按
         transaction_date 分一层组——每天一个数字，按月聚合交给前端做 -->
    <select id="aggregateWinLossByRoleAndDate" resultType="com.eazycount.dto.DashboardTrendPointDTO$RoleAmount">
        SELECT x.date AS date, x.role AS role, COALESCE(SUM(x.amt), 0) AS amount
        FROM (
            SELECT
                t.transaction_date AS date,
                a.role AS role,
                CASE
                    WHEN t.transaction_type = 'WIN' THEN t.amount
                    WHEN t.transaction_type = 'LOSE' THEN -t.amount
                    WHEN t.transaction_type = 'ADJUSTMENT' THEN t.amount
                    ELSE 0
                END AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type IN ('WIN', 'LOSE', 'ADJUSTMENT')
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            SELECT t.transaction_date AS date, a.role AS role, -t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'PROFIT'
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            SELECT t.transaction_date AS date, a.role AS role, t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.from_account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.from_account_id IS NOT NULL
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'PROFIT'
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            <!-- RATE 中间人手续费单边行：account_id=middleman, from_account_id 为空 → +amount -->
            SELECT t.transaction_date AS date, a.role AS role, t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'RATE'
              AND t.from_account_id IS NULL
              AND <include refid="dashboardRateMiddlemanFeeDescription"/>
              AND <include refid="dashboardRateMiddlemanFeeLeg"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            <!-- RATE 中间人手续费旧版两边行：from_account_id=middleman → +amount -->
            SELECT t.transaction_date AS date, a.role AS role, t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.from_account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.from_account_id IS NOT NULL
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'RATE'
              AND <include refid="dashboardRateMiddlemanFeeLeg"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>
        ) x
        GROUP BY x.date, x.role
    </select>

    <!-- Cr/Dr 桶（Trend Chart 用）：跟 aggregateCrDrByRole 完全同一套规则（CLEAR 同样排除），
         只是多按 transaction_date 分一层组 -->
    <select id="aggregateCrDrByRoleAndDate" resultType="com.eazycount.dto.DashboardTrendPointDTO$RoleAmount">
        SELECT x.date AS date, x.role AS role, COALESCE(SUM(x.amt), 0) AS amount
        FROM (
            SELECT t.transaction_date AS date, a.role AS role, -t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type IN ('PAYMENT', 'CLAIM', 'CONTRA', 'RATE')
              AND <include refid="dashboardRateTransferLegOnly"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            SELECT t.transaction_date AS date, a.role AS role, t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.from_account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.from_account_id IS NOT NULL
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type IN ('PAYMENT', 'CLAIM', 'CONTRA', 'RATE')
              AND <include refid="dashboardRateTransferLegOnly"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>

            UNION ALL

            <!-- RATE 中间人手续费付款方视角：account_id=付款方、from_account_id 非空 → -amount -->
            SELECT t.transaction_date AS date, a.role AS role, -t.amount AS amt
            FROM transactions t
            INNER JOIN account a ON a.id = t.account_id
            INNER JOIN currency c ON c.id = t.currency_id AND c.tenant_id = t.tenant_id
            WHERE t.tenant_id IN
            <foreach collection="tenantIds" item="tid" open="(" separator="," close=")">
                #{tid}
            </foreach>
              AND t.from_account_id IS NOT NULL
              AND t.approval_status = 'APPROVED'
              AND t.transaction_date &gt;= #{dateFrom}
              AND t.transaction_date &lt;= #{dateTo}
              AND t.transaction_type = 'RATE'
              AND <include refid="dashboardRateMiddlemanFeeLeg"/>
              AND UPPER(c.code) = UPPER(#{currencyCode})
              AND UPPER(a.role) IN
              <foreach collection="roles" item="r" open="(" separator="," close=")">
                  UPPER(#{r})
              </foreach>
        ) x
        GROUP BY x.date, x.role
    </select>

    <!-- 当前（live）股权行：Earnings 卡片乘数来源，当月/未指定历史月份时查询 -->
    <select id="findLiveOwnership" resultType="com.eazycount.entity.TenantOwnership">
        SELECT
            id,
            tenant_id AS tenantId,
            account_id AS accountId,
            owner_type AS ownerType,
            partner_tenant_id AS partnerTenantId,
            percentage,
            read_only AS readOnly,
            sort_order AS sortOrder
        FROM tenant_ownership
        WHERE tenant_id = #{tenantId}
          AND account_id = #{accountId}
          AND owner_type = #{ownerType}
        LIMIT 1
    </select>

    <!-- 历史股权快照行：查过去月份的 Dashboard 数字时用，effectiveMonth 传该月第一天 -->
    <select id="findHistoricalOwnership" resultType="com.eazycount.entity.TenantOwnershipHistory">
        SELECT
            id,
            tenant_id AS tenantId,
            effective_month AS effectiveMonth,
            account_id AS accountId,
            owner_type AS ownerType,
            partner_tenant_id AS partnerTenantId,
            percentage,
            read_only AS readOnly,
            saved_by AS savedBy,
            saved_at AS savedAt
        FROM tenant_ownership_history
        WHERE tenant_id = #{tenantId}
          AND account_id = #{accountId}
          AND owner_type = #{ownerType}
          AND effective_month = #{effectiveMonth}
        LIMIT 1
    </select>

    <!-- 公司->Group 股权分配行（当月/未指定历史月份）：一条 SQL 批量拿到 Group 下每家公司分配给
         该 Group 的百分比，用来给每家公司的 Net Profit 加权，汇总成 Group Profit -->
    <select id="findGroupEquityPercentages" resultType="com.eazycount.entity.TenantOwnership">
        SELECT
            id,
            tenant_id AS tenantId,
            account_id AS accountId,
            owner_type AS ownerType,
            partner_tenant_id AS partnerTenantId,
            percentage,
            read_only AS readOnly,
            sort_order AS sortOrder
        FROM tenant_ownership
        WHERE tenant_id IN
        <foreach collection="companyTenantIds" item="tid" open="(" separator="," close=")">
            #{tid}
        </foreach>
          AND owner_type = 'group'
          AND partner_tenant_id = #{groupTenantId}
    </select>

    <!-- 公司->Group 股权分配行（历史快照）：同上，effectiveMonth 传该月第一天 -->
    <select id="findHistoricalGroupEquityPercentages" resultType="com.eazycount.entity.TenantOwnershipHistory">
        SELECT
            id,
            tenant_id AS tenantId,
            effective_month AS effectiveMonth,
            account_id AS accountId,
            owner_type AS ownerType,
            partner_tenant_id AS partnerTenantId,
            percentage,
            read_only AS readOnly,
            saved_by AS savedBy,
            saved_at AS savedAt
        FROM tenant_ownership_history
        WHERE tenant_id IN
        <foreach collection="companyTenantIds" item="tid" open="(" separator="," close=")">
            #{tid}
        </foreach>
          AND owner_type = 'group'
          AND partner_tenant_id = #{groupTenantId}
          AND effective_month = #{effectiveMonth}
    </select>

    <!-- 【Trend Chart 的 Earnings 走势线用，Company/Group 通用】一次性批量查"当前登录身份在
         这一个 tenant 上"一批历史月份各自的股权% —— 一条 effective_month IN (...) 查完，
         不按月份循环发 SQL。tenantId 传公司自己的 id 或 Group 自己的 id 都行，这张表本来就
         不区分 tenant_id 指向的是公司还是 Group。 -->
    <select id="findOwnershipPercentagesByMonths" resultType="com.eazycount.entity.TenantOwnershipHistory">
        SELECT
            id,
            tenant_id AS tenantId,
            effective_month AS effectiveMonth,
            account_id AS accountId,
            owner_type AS ownerType,
            partner_tenant_id AS partnerTenantId,
            percentage,
            read_only AS readOnly,
            saved_by AS savedBy,
            saved_at AS savedAt
        FROM tenant_ownership_history
        WHERE tenant_id = #{tenantId}
          AND account_id = #{accountId}
          AND owner_type = #{ownerType}
          AND effective_month IN
          <foreach collection="effectiveMonths" item="m" open="(" separator="," close=")">
              #{m}
          </foreach>
    </select>

    <!-- 【Group Profit 走势线用】findGroupEquityPercentages 的"按月批量"版本——一批子公司、
         一批历史月份，一条 effective_month IN (...) 查完，不按月份循环查询。 -->
    <select id="findGroupEquityPercentagesByMonths" resultType="com.eazycount.entity.TenantOwnershipHistory">
        SELECT
            id,
            tenant_id AS tenantId,
            effective_month AS effectiveMonth,
            account_id AS accountId,
            owner_type AS ownerType,
            partner_tenant_id AS partnerTenantId,
            percentage,
            read_only AS readOnly,
            saved_by AS savedBy,
            saved_at AS savedAt
        FROM tenant_ownership_history
        WHERE tenant_id IN
        <foreach collection="companyTenantIds" item="tid" open="(" separator="," close=")">
            #{tid}
        </foreach>
          AND owner_type = 'group'
          AND partner_tenant_id = #{groupTenantId}
          AND effective_month IN
          <foreach collection="effectiveMonths" item="m" open="(" separator="," close=")">
              #{m}
          </foreach>
    </select>

</mapper>
