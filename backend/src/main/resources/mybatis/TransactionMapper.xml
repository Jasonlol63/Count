<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eazycount.dao.TransactionDao">

    <insert id="insert" useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        INSERT INTO transactions (
            tenant_id, transaction_type, account_id, from_account_id, currency_id, amount, transaction_date, description,
            remark,created_by, updated_by,approval_status,approved_by,approved_at,bank_process_posted_id,rate_group_id)
        VALUES (
                #{tenantId}, #{transactionType}, #{accountId},  #{fromAccountId},  #{currencyId}, #{amount},
                #{transactionDate},#{description}, #{remark}, #{createdBy}, #{updatedBy}, #{approvalStatus},
                #{approvedBy}, #{approvedAt}, #{bankProcessPostedId}, #{rateGroupId}
               )
    </insert>

    <!-- Delete-process guard: any transactions recorded under this process's data captures? -->
    <select id="countTransactionsByProcessId" resultType="int">
        SELECT COUNT(*)
        FROM transactions t
                 INNER JOIN data_capture_line dcl ON dcl.transaction_id = t.id
                 INNER JOIN data_captures dc ON dc.id = dcl.capture_id
        WHERE dc.process_id = #{processId}
          AND t.tenant_id = #{tenantId}
    </select>

    <!-- Delete-account guard: any transactions referencing this account (as payer or transfer-from)? -->
    <select id="countTransactionsByAccountId" resultType="int">
        SELECT COUNT(*)
        FROM transactions t
        WHERE t.tenant_id = #{tenantId}
          AND (t.account_id = #{accountId} OR t.from_account_id = #{accountId})
    </select>

    <!-- Delete-currency guard: any transactions recorded under this currency? -->
    <select id="countTransactionsByCurrencyId" resultType="int">
        SELECT COUNT(*)
        FROM transactions t
        WHERE t.tenant_id = #{tenantId}
          AND t.currency_id = #{currencyId}
    </select>

</mapper>
